<h1>つぶやき一覧</h1>
   <table>
     <tr>
       <th>つぶやき</th>
     </tr>
<% @apps.each do |app| %>
  <tr>
    <td><%= app.content %></td>
    <td><%= link_to '詳細を確認する', app_path(app.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくつぶやく', new_app_path %>
